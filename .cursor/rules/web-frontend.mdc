---
description: Web app React/TypeScript conventions
globs: "web/**"
alwaysApply: false
---

# Web frontend

## Stack

Vite + React + TypeScript. No router — phase state in `App.tsx`.

## Inference modes

- Default: `mockAccentOracleClient` in `accentOracleClient.ts`
- API: `VITE_ACCENT_ORACLE_MODE=api` + `VITE_ACCENT_ORACLE_API_URL`
- Dev tools: `VITE_ACCENT_ORACLE_DEV=1` or `?dev=1` (`devFlags.ts` / `localStorage`) — CPU/validation diagnostic copy + mock/API toggle; off by default

## UI language

User-facing strings in **Catalan**. Keep accessibility (`aria-*`, `aria-live` on analysis status).

## Results map

`ResultsMapStage` drives `DialectMap` on `map-oracle-linework.svg`: linework stage, one selected comarca, ranking sync. Scores remain the model source of truth for the sidebar; the map focuses a hotspot comarca per dialect (`dialectHotspots.ts`).

## Validation flow

API mode: `needsValidation()` skips a second take only when top score ≥ 0.50 and top-two gap ≥ 0.15; otherwise pick a different prompt from `prompts.ts` → `pickBetterResult()`. Send `promptId` / `promptText` with each `/analyze`.

## Checks after changes

```bash
cd web && npm run lint && npm run build
```
